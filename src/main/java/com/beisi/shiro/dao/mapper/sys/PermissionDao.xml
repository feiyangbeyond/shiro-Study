<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beisi.shiro.dao.sys.PermissionDao">
	
	<select id="getPermissionByid" resultType="com.beisi.shiro.model.sys.Permission">
		select p.id,p.pname,p.pathUrl,p.sortNum,p.add_date,p.up_date from 
			t_role r,t_role_permission rp,t_permission p
		where 
			r.id=rp.roleId and rp.perId=p.id
		and 
			r.id=#{rid}
	</select>
	
	<select id="getAllPermissions" resultType="com.beisi.shiro.model.sys.Permission">
		select * from t_permission order by sortNum
	</select>
	
	

</mapper>