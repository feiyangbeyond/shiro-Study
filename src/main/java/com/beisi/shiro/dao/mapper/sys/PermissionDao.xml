<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beisi.shiro.dao.sys.PermissionDao">
	
	<select id="getPermissionsByUid" resultType="String">
		select p.pname from 
			t_user u,t_role r,t_user_role ur,t_role_permission rp,t_permission p
		where 
			u.id=ur.userId and ur.roleId=r.id and r.id=rp.roleId and rp.perId=p.id
		and 
			u.id=#{uid}
	</select>
	
	<select id="getAllPermissions" resultType="com.beisi.shiro.model.sys.Permission">
		select * from t_permission order by sortNum
	</select>
	
	

</mapper>