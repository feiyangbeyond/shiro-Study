<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.beisi.shiro.dao.sys.BaseDao">

	<!-- 按id查找一个 -->
	<select id="select" resultType="map">
		select * from ${tableName} where id=#{id}
	</select>

	<!-- 查询所有 -->
	<select id="selectAll" resultType="map">
		select * from ${tableName}
	</select>

	<insert id="add">
		insert into ${tableName} values
		<foreach collection="objects" item="param" open="("	separator="," close=")">
			#{param}
		</foreach>
		<!-- 生成的sql类似于insert into t_user values (?,?,?....) -->
	</insert>
	
	<insert id="addForNotMatch">
		insert into ${tableName} 
		<foreach collection="fielsNames" item="param" open="("	separator="," close=")">
			${param}
		</foreach>
		values
		<foreach collection="fieldValues" item="param" open="("	separator="," close=")">
			#{param}
		</foreach>
	</insert>

	<update id="update">
		update ${tableName}
		<set>
			<foreach collection="objects" item="param">
				${param},
			</foreach>
		</set>
		where id=#{id}
	</update>

	<delete id="delete">
		delete from ${tableName} where id=#{id}
	</delete>

</mapper>